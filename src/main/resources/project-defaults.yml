swarm:
  logging:
    pattern-formatters:
      MY_PATTERN:
        pattern: "%K{level}%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c] (%t) (ZZ:%X{ZZZZ}) %s%e%n"
    console-handlers:
      CONSOLE:
        named-formatter: MY_PATTERN
